import { Center, Root, Text } from "../System/Lib/Widgets";

export default function Login() {
  return Root({
    child: Center({
      child: Text("Login page is maintenance")
    })
  }).builder();
}